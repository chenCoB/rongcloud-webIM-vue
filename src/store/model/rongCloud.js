
const state = {
     status: false,
};

const actions = {

};
const mutations = {
    setStatus(state, status) {
        state.status = status;
    }
};

export default  {
    state,
    actions,
    mutations
}
